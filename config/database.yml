default: &default
  adapter: postgresql
  pool: 10
  timeout: 5000
  checkout_timeout: 5  # 添加连接检出超时
  reaping_frequency: 10  # 添加连接回收频率（秒)
  host: work_design-postgres
  username: postgres
  password: <%= ENV['POSTGRES_PASSWORD'] %>
development:
  primary:
    <<: *default
    database: work_development
    gssencmode: disable
  queue:
    <<: *default
    database: work_queue_development
    migrations_paths: db/queue_migrate
    gssencmode: disable
  cache:
    <<: *default
    database: work_cache_development
    migrations_paths: db/cache_migrate
  prod:
    <<: *default
    database: work_prod
test:
  primary:
    <<: *default
    database: work_test
  queue:
    <<: *default
    database: work_test
  cache:
    <<: *default
    database: work_test
  prod:
    <<: *default
    database: work_prod
production:
  primary:
    <<: *default
    database: work_production
  queue:
    <<: *default
    database: work_queue_production
    migrations_paths: db/queue_migrate
  cache:
    <<: *default
    database: work_cache_production
    migrations_paths: db/cache_migrate
  prod:
    <<: *default
    database: work_production