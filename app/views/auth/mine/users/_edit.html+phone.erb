<%= form_with theme: 'default_phone', model: @user, url: my_user_path do |f| %>
  <%= hidden_field_tag :return_to, params[:return_to] %>
  <%= f.text_field :name, required: true %>
  <%= f.submit %>
<% end %>
